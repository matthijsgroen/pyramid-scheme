# Principles

This tier is for design-token and foundation documentation — colors, spacing, typography — as standalone Storybook docs, not tied to a single component. Nothing lives here yet: the game's colors and spacing values are still inlined per-component (e.g. difficulty colors repeated across `DifficultyPill`, `backdropSelection.ts`, `tombImageMap.ts`). Add foundation docs here once those values are centralized into shared tokens.
